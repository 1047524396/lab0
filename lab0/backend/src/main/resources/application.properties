# 数据源配置
spring.datasource.url=jdbc:mysql://8.217.196.247:3306/your-database-name
spring.datasource.username=webstarter001
spring.datasource.password=10bac40b363b4111

# MySQL连接池配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.initialSize=5
spring.datasource.maxActive=10
spring.datasource.maxIdle=5
spring.datasource.minIdle=2
spring.datasource.maxWait=60000

# MyBatis配置
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.use-generated-keys=true
mybatis.configuration.use-column-label=true
mybatis.configuration.jdbc-type-for-null=NULL

# 服务器端口配置
server.port=8080

# Vue.js前端配置
# 配置前端静态资源路径
spring.resources.static-locations=classpath:/public/,classpath:/static/,classpath:/frontend/dist/

# 配置跨域
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.location=classpath:/static/

# Element Plus样式主题
element-plus.theme=light

# Vue Router模式
spring.mvc.servlet.path=/

# Spring Boot Actuator配置
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always

